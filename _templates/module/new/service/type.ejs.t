---
to: src/modules/<%= name %>/service/<%= name %>.type.js
unless_exists: true
---
module.exports = function(prisma, service) {
  // relationships and other type level resolvers go gere

  return {
    
  }
}